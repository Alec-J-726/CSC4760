LLM environment
LLM: ChatGPT, GPT‑5.1 (OpenAI)

Interface: Cursor IDE integrated chat

Language used: C++ with MPI (mpi.h)

Prompt: "Write a C++ MPI program that passes one integer around a logical ring of processes (MPI_COMM_WORLD). The integer starts at 0 on rank 0 and is incremented each time it is received; the number of laps N should be set at compile time (e.g. -DRING_LAPS=5). Only rank 0 prints the final token and expected value. Handle world size 1. Here is the problems that I was given: Problem 1 (15pts/12pts):
Write an MPI program (by hand, no LLMs) that passes a message of one integer around in a logical ring of
processes with MPI COMM WORLD. The integer should start at 0 in process 0 and be incremented each time it
passes around the ring, and you should be able to have the message go around the ring N times, where N
is specified at compile time. Problem 2 (15pts/12pts): Using your favorite LLM, vibe code Problem 1, and compare and contrast with what you developed by hand for correctness/functionality. Show the sequence of prompts you devised and applied, together the sequence of code produced, and the final product. Report what version and details of your LLM environment too. If it is incorrect after several prompt rounds, please explain what the LLM seems to be getting wrong."

Cursor then created the CPP files in the directory